LOG:  database system was shut down at 2017-02-20 02:13:18 UTC
LOG:  MultiXact member wraparound protections are now enabled
LOG:  database system is ready to accept connections
LOG:  autovacuum launcher started
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username char[16],
		
		password char[16]
	);
FATAL:  database "lostdb" does not exist
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  using stale statistics instead of current ones because stats collector is not responding
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not bind IPv6 socket: Address already in use
HINT:  Is another postmaster already running on port 5432? If not, wait a few seconds and retry.
LOG:  could not bind IPv4 socket: Address already in use
HINT:  Is another postmaster already running on port 5432? If not, wait a few seconds and retry.
WARNING:  could not create listen socket for "localhost"
FATAL:  could not create any TCP/IP sockets
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  using stale statistics instead of current ones because stats collector is not responding
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open file "postmaster.pid": No such file or directory
LOG:  performing immediate shutdown because data directory lock file is invalid
LOG:  received immediate shutdown request
LOG:  could not open temporary statistics file "pg_stat/global.tmp": No such file or directory
WARNING:  terminating connection because of crash of another server process
DETAIL:  The postmaster has commanded this server process to roll back the current transaction and exit, because another server process exited abnormally and possibly corrupted shared memory.
HINT:  In a moment you should be able to reconnect to the database and repeat your command.
LOG:  database system was shut down at 2017-02-20 02:16:09 UTC
LOG:  MultiXact member wraparound protections are now enabled
LOG:  database system is ready to accept connections
LOG:  autovacuum launcher started
FATAL:  database "322db" does not exist
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username char[16],
		
		password char[16]
	);
FATAL:  lock file "postmaster.pid" already exists
HINT:  Is another postmaster (PID 229) running in data directory "/home/osnapdev/nd/322/lostdb"?
LOG:  database system was interrupted; last known up at 2017-02-20 04:36:07 UTC
LOG:  database system was not properly shut down; automatic recovery in progress
LOG:  redo starts at 0/18C1950
LOG:  invalid record length at 0/18D8430
LOG:  redo done at 0/18D83F8
LOG:  last completed transaction was at log time 2017-02-20 04:37:30.027909+00
LOG:  MultiXact member wraparound protections are now enabled
LOG:  database system is ready to accept connections
LOG:  autovacuum launcher started
LOG:  could not open temporary statistics file "pg_stat_tmp/global.tmp": No such file or directory
LOG:  could not open file "postmaster.pid": No such file or directory
LOG:  performing immediate shutdown because data directory lock file is invalid
LOG:  received immediate shutdown request
WARNING:  terminating connection because of crash of another server process
DETAIL:  The postmaster has commanded this server process to roll back the current transaction and exit, because another server process exited abnormally and possibly corrupted shared memory.
HINT:  In a moment you should be able to reconnect to the database and repeat your command.
WARNING:  terminating connection because of crash of another server process
DETAIL:  The postmaster has commanded this server process to roll back the current transaction and exit, because another server process exited abnormally and possibly corrupted shared memory.
HINT:  In a moment you should be able to reconnect to the database and repeat your command.
LOG:  database system was shut down at 2017-02-27 01:09:48 UTC
LOG:  MultiXact member wraparound protections are now enabled
LOG:  database system is ready to accept connections
LOG:  autovacuum launcher started
FATAL:  lock file "postmaster.pid" already exists
HINT:  Is another postmaster (PID 1213) running in data directory "/home/osnapdev/nd/322/lostdb"?
ERROR:  syntax error at or near "role" at character 103
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16)
		role integer REFERENCES roles
	);
ERROR:  syntax error at or near "role_fk" at character 103
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16)
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  syntax error at or near "tables" at character 6
STATEMENT:  drop tables;
ERROR:  syntax error at or near "users" at character 6
STATEMENT:  drop users;
ERROR:  syntax error at or near "*" at character 12
STATEMENT:  drop table *;
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  missing FROM-clause entry for table "role" at character 27
STATEMENT:  SELECT * FROM roles WHERE role.rolename = 'Official Guy'
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  syntax error at or near "VALUE" at character 30
STATEMENT:  INSERT INTO roles (rolename) VALUE Superior Being
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  syntax error at or near "Superior" at character 37
STATEMENT:  INSERT INTO roles (rolename) VALUES Superior Being
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  syntax error at or near "'Superior Being'" at character 37
STATEMENT:  INSERT INTO roles (rolename) VALUES 'Superior Being'
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  syntax error at or near "Being" at character 47
STATEMENT:  INSERT INTO roles (rolename) VALUES (Superior Being)
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  unterminated quoted identifier at or near """ at character 60
STATEMENT:  SELECT * FROM roles WHERE roles.rolename = 'Superior Being'"
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  syntax error at or near "WHERE" at character 41
STATEMENT:  select * from users JOIN roles.rolename WHERE users.role_fk = roles.role_pk;
ERROR:  syntax error at or near "WHERE" at character 41
STATEMENT:  select * from users JOIN roles.rolename WHERE role_fk = role_pk;
ERROR:  syntax error at or near "WHERE" at character 32
STATEMENT:  select * from users JOIN roles WHERE role_fk = role_pk;
ERROR:  syntax error at or near "WHERE" at character 38
STATEMENT:  select * from users INNER JOIN roles WHERE role_fk = role_pk;
ERROR:  relation "roles.rolename" does not exist at character 22
STATEMENT:  select * from users, roles.rolename WHERE role_fk = role_pk;
ERROR:  column "role_pk" does not exist at character 37
HINT:  Perhaps you meant to reference the column "users.role_fk".
STATEMENT:  select * from users WHERE role_fk = role_pk;
ERROR:  column "role_pk" does not exist at character 43
HINT:  Perhaps you meant to reference the column "users.role_fk".
STATEMENT:  select * from users WHERE users.role_fk = role_pk;
ERROR:  missing FROM-clause entry for table "roles" at character 43
STATEMENT:  select * from users WHERE users.role_fk = roles.role_pk;
ERROR:  missing FROM-clause entry for table "users" at character 8
STATEMENT:  select users.username, roles.rolename WHERE users.role_fk = roles.role_pk;
ERROR:  column roles.role_pk does not exist at character 79
HINT:  Perhaps you meant to reference the column "roles.roles_pk".
STATEMENT:  select users.username, roles.rolename FROM users, roles WHERE users.role_fk = roles.role_pk;
ERROR:  column "role_pk" does not exist at character 73
HINT:  Perhaps you meant to reference the column "users.role_fk" or the column "roles.roles_pk".
STATEMENT:  select users.username, roles.rolename FROM users, roles WHERE role_fk = role_pk;
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  syntax error at end of input at character 74
STATEMENT:  SELECT * FROM facilities WHERE (name = 'Arkensassafrass' OR code = 'POOP'
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  column "facility_pk" does not exist at character 8
STATEMENT:  SELECT facility_pk FROM facilities WHERE facilities.name = 'Tubers'
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facilities serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  column "facilities_pk" does not exist at character 8
HINT:  Perhaps you meant to reference the column "facilities.facilities".
STATEMENT:  SELECT facilities_pk FROM facilities WHERE facilities.name = 'Tubers'
ERROR:  cannot drop table facilities because other objects depend on it
DETAIL:  constraint assets_facility_pk_fkey on table assets depends on table facilities
HINT:  Use DROP ... CASCADE to drop the dependent objects too.
STATEMENT:  drop table facilities ;
ERROR:  syntax error at or near "cascase" at character 23
STATEMENT:  drop table facilities cascase;
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  column "facilities_pk" does not exist at character 8
HINT:  Perhaps you meant to reference the column "facilities.facility_pk".
STATEMENT:  SELECT facilities_pk FROM facilities WHERE facilities.name = 'Princeton, NJ'
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_pk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  column "facility_fk" of relation "assets" does not exist at character 39
STATEMENT:  INSERT INTO assets (tag, description, facility_fk) VALUES ('SBX', 'Startbucks coffee', (3))
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  syntax error at or near "." at character 52
STATEMENT:  SELECT roles.rolename FROM users, roles WHERE (user.username = 'asdf' AND user.role_fk = roles.role_pk
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  syntax error at end of input at character 105
STATEMENT:  SELECT roles.rolename FROM users, roles WHERE (users.username = 'asdf' AND users.role_fk = roles.role_pk
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  missing FROM-clause entry for table "asset" at character 28
STATEMENT:  SELECT * FROM assets WHERE asset.tag = 'SBX'
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  syntax error at or near "2017" at character 56
STATEMENT:  UPDATE assets SET facility_fk = 'NULL', arrival_dt = ''2017-03-23T23:23:00'::timestamp' WHERE assets.tag = ''SBX''
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  invalid input syntax for integer: "NULL" at character 33
STATEMENT:  UPDATE assets SET facility_fk = 'NULL', arrival_dt = '2017-03-23T23:23:00'::timestamp WHERE assets.tag = 'SBX'
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  syntax error at or near "WHERE" at character 130
STATEMENT:  SELECT * FROM assets WHERE (assets.arrive_dt >= '2017-01-01T00:12:00'::timestamp AND assets.facility_fk = facilities.facility_pk WHERE facilities.name = 'Princeton, NJ'
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  column assets.arrive_dt does not exist at character 29
HINT:  Perhaps you meant to reference the column "assets.arrival_dt".
STATEMENT:  SELECT * FROM assets WHERE (assets.arrive_dt >= '2017-01-01T00:12:00'::timestamp AND assets.facility_fk = (3))
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
LOG:  could not bind IPv6 socket: Address already in use
HINT:  Is another postmaster already running on port 5432? If not, wait a few seconds and retry.
LOG:  could not bind IPv4 socket: Address already in use
HINT:  Is another postmaster already running on port 5432? If not, wait a few seconds and retry.
WARNING:  could not create listen socket for "localhost"
FATAL:  could not create any TCP/IP sockets
LOG:  database system was shut down at 2017-02-06 11:03:36 UTC
LOG:  MultiXact member wraparound protections are now enabled
LOG:  database system is ready to accept connections
LOG:  autovacuum launcher started
FATAL:  database "lost" does not exist
FATAL:  database "lost" does not exist
FATAL:  database "lost" does not exist
ERROR:  column roles.rolename does not exist at character 27
HINT:  Perhaps you meant to reference the column "roles.rolesname".
STATEMENT:  SELECT * FROM roles WHERE roles.rolename = 'Logistics Officer'
ERROR:  database "lost" is being accessed by other users
DETAIL:  There is 1 other session using the database.
STATEMENT:  DROP DATABASE lost;
LOG:  could not send data to client: Broken pipe
STATEMENT:  DROP DATABASE lost;
FATAL:  connection to client lost
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
LOG:  database system was interrupted; last known up at 2017-03-02 20:31:12 UTC
LOG:  database system was not properly shut down; automatic recovery in progress
LOG:  invalid record length at 0/179FB98
LOG:  redo is not required
LOG:  MultiXact member wraparound protections are now enabled
LOG:  database system is ready to accept connections
LOG:  autovacuum launcher started
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  column roles.pk does not exist at character 40
STATEMENT:  SELECT roles.rolename FROM roles WHERE roles.pk = '1';
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  column assets.asset_pk does not exist at character 39
HINT:  Perhaps you meant to reference the column "assets.assets_pk".
STATEMENT:  SELECT assets.tag FROM assets WHERE ((assets.asset_pk = transfers.asset_fk) AND (transfers.load_dt = NULL OR transfers.unload_dt = NULL))
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  missing FROM-clause entry for table "transfers" at character 58
STATEMENT:  SELECT assets.tag FROM assets WHERE ((assets.assets_pk = transfers.asset_fk) AND (transfers.load_dt = NULL OR transfers.unload_dt = NULL))
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		dest_fk integer REFERENCES facilities,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "intransits" already exists
STATEMENT:  CREATE TABLE intransits (
		intransit_pk serial primary key,
		source_fk integer REFERENCES facilities,
		load_dt timestamp,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp
	);
ERROR:  column transfers.load_dt does not exist at character 94
STATEMENT:  SELECT assets.tag FROM assets, transfers WHERE ((assets.assets_pk = transfers.asset_fk) AND (transfers.load_dt = NULL OR transfers.unload_dt = NULL))
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		load_dt timestamp NULL,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp NULL,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		load_dt timestamp NULL,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp NULL,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		load_dt timestamp NULL,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp NULL,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  missing FROM-clause entry for table "transfer" at character 93
STATEMENT:  SELECT assets.tag FROM assets, transfers WHERE (assets.assets_pk = transfers.asset_fk) AND (transfer.load_dt = NULL)
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		load_dt timestamp NULL,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp NULL,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		load_dt timestamp NULL,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp NULL,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  invalid input syntax for type timestamp: "" at character 113
STATEMENT:  SELECT assets.tag FROM assets, transfers WHERE (assets.assets_pk = transfers.asset_fk) AND (transfers.load_dt = '')
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		load_dt timestamp NULL,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp NULL,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		load_dt timestamp NULL,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp NULL,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		load_dt timestamp NULL,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp NULL,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		load_dt timestamp NULL,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp NULL,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  column facilities.facility_fk does not exist at character 122
HINT:  Perhaps you meant to reference the column "facilities.facility_pk".
STATEMENT:  SELECT assets.tag, facilities.name FROM assets, facilities, transfers WHERE (assets.assets_pk = transfers.asset_fk) AND (facilities.facility_fk = transfers.dest_fk) AND (transfers.approver_fk IS NULL)
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		load_dt timestamp NULL,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp NULL,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  column facilities.facility_fk does not exist at character 122
HINT:  Perhaps you meant to reference the column "facilities.facility_pk".
STATEMENT:  SELECT assets.tag, facilities.name FROM assets, facilities, transfers WHERE (assets.assets_pk = transfers.asset_fk) AND (facilities.facility_fk = transfers.dest_fk) AND (transfers.approver_fk IS NULL)
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		load_dt timestamp NULL,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp NULL,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  invalid input syntax for integer: "Los Angeles" at character 122
STATEMENT:  UPDATE transfers SET approver_fk = 2, approve_dt = '2017-03-06T01:34:02.925539'::timestamp WHERE transfers.transfer_pk = 'Los Angeles'
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		load_dt timestamp NULL,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp NULL,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
ERROR:  relation "roles" already exists
STATEMENT:  CREATE TABLE roles (
		roles_pk serial primary key,
		rolename varchar(25)
	);
ERROR:  relation "facilities" already exists
STATEMENT:  CREATE TABLE facilities (
		facility_pk serial primary key,
		name varchar(32),
		code varchar(6)
	);
ERROR:  relation "users" already exists
STATEMENT:  CREATE TABLE users (
		
		user_pk serial primary key,
		
		username varchar(16),
		
		password varchar(16),
		role_fk integer REFERENCES roles
	);
ERROR:  relation "assets" already exists
STATEMENT:  CREATE TABLE assets (
		assets_pk serial primary key,
		tag varchar(16),
		description text,
		facility_fk integer REFERENCES facilities NULL,
		arrival_dt timestamp
	);
ERROR:  relation "transfers" already exists
STATEMENT:  CREATE TABLE transfers (
		transfer_pk serial primary key,
		requester_fk integer REFERENCES users,
		submit_dt timestamp,
		source_fk integer REFERENCES facilities,
		load_dt timestamp NULL,
		dest_fk integer REFERENCES facilities,
		unload_dt timestamp NULL,
		asset_fk integer REFERENCES assets,
		approver_fk integer REFERENCES users,
		approve_dt timestamp
	);
